# AOS MCP Servers — Copilot Instructions

Config-driven MCP server deployment on Azure Functions.
Deploy MCP servers from JSON configuration — no code changes needed.

## Key Patterns
- Server registry is JSON-based (see example_mcp_server_registry.json)
- Schema defined in mcp_server_schema.py
