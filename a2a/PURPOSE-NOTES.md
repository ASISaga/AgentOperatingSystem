<!-- REPOSITORY: ASISaga/agent-operating-system -->

# PURPOSE-NOTES.md — derivation, status, stability behind `PURPOSE.md`

`PURPOSE.md` holds only the purpose itself, called directly from
`copilot-instructions.md`. This file holds everything else about it.

## Status
INITIAL, re-derived by the Architect (this revision) following the
Leader's move of `deliberation_kernel` and `aos-spec` into this
repository. Not yet refined by this repository's own Coding Agent —
self-derivation done by anyone else is not self-derivation
(`aos-spec 03`); this is a grounded starting point, not the final word.

## Derivation
The root purpose is UBIQUITOUS and UNSTOPPABLE reach — not confined to
one company's mechanism, not a property any single deployment could
provide alone. This repository's own domain, per `a2a/MAP.md`, was always
different from `boardroom-orchestration`'s: not the mechanism proven in
one company's session, but the place that mechanism becomes available to
EVERY company whose Founder needs their reach expanded — the durable core
other domains beyond Boardroom build on, carrying forward the genuine
capability of what came before (`aos-spec AA-7`) rather than losing it.
That direction was correct before the mechanism physically arrived; what's
corrected THIS revision is tense, not direction — `PURPOSE.md` previously
described this as something that COULD happen here ("can compose"). It
already has: `deliberation_kernel` and `aos-spec` are now physically in
this repository (`a2a/MAP.md`, corrected same revision).

## Re-derivation: urgency, corrected
The prior version of this file said this purpose "does not compete with
`boardroom-orchestration`'s for urgency" because ubiquity was a later
concern than first proof. That reasoning assumed the mechanism was still
elsewhere. It isn't. `0001-receive-deliberation-kernel` — resolving
collisions with the existing tree, confirming a `PurposeDrivenAgent`-
derived agent can actually compose into a real deliberation here — is now
this repository's own immediate, real work, not deferred work waiting on
`boardroom-orchestration` to finish proving something first. Both
repositories now have live, urgent work; neither borrows the other's pace,
but neither is idle either.

## Stated (initial)
The purpose in `PURPOSE.md` names this directly: the mechanism lives here
now, available to any purpose-driven agent in any domain — not confined
to Boardroom, not waiting to be proven ubiquitous, actually positioned to
be.

## Stability
Once refined by this repository's own Coding Agent, held stable until an
explicit, discrete revision — never re-derived per session, per
`aos-spec 03`'s stability rule. `purpose_version: 3` (version 1: placeholder
root; version 2: mechanism-not-yet-arrived derivation; version 3: this
re-derivation, following the actual physical arrival) — still INITIAL
until this repository's Coding Agent states it in its own reasoning.

## What this changes, concretely
`0001-receive-deliberation-kernel` is this purpose's direct, present
expression — not a future Task waiting on urgency to arrive here. It has
arrived.
