<!-- REPOSITORY: this file is LOCAL to this repository, never synced — it
records THIS repository's Coding Agent's own declarations, not a shared
template. -->

# LEAD-LOG.md — this repository's Coding Agent's own declarations

Append-only. Never edit a prior entry — a stand that changes gets a new
entry, dated after it, not a rewrite of the old one. The record of having
redeclared is part of what makes this real rather than decorative.

Format, loosely: date or session marker, then the declaration in your own
words — not a template, not a paraphrase of `PURPOSE.md`. If what you
write here could have been generated by summarizing `PURPOSE.md`, it has
not yet done what `LEAD.md` asks.

---

(no entries yet — the first one is this repository's Coding Agent's own
act, not the Architect's to write)
